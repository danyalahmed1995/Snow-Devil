import { describe,expect,it } from 'vitest';
import { activeNotifications,effectiveUnread,normalizeNotifications,type NativeNotification } from './notification-store';
const record:NativeNotification={id:'1',unread:true,reason:'mention',updatedAt:'2026-01-01T00:00:00Z',subject:{title:'Mention',type:'Issue'},repository:{fullName:'owner/repo'}};
describe('native notification state',()=>{it('supports local read overrides without a GitHub mutation',()=>{expect(effectiveUnread(record,{})).toBe(true);expect(effectiveUnread(record,{'1':true})).toBe(false);expect(effectiveUnread(record,{'1':false})).toBe(true)});it('filters active snoozes deterministically',()=>{expect(activeNotifications([record],{'1':'2026-01-02T00:00:00Z'},new Date('2026-01-01').getTime())).toEqual([]);expect(activeNotifications([record],{'1':'2025-12-31T00:00:00Z'},new Date('2026-01-01').getTime())).toEqual([record])})});
describe('notification normalization',()=>{it('keeps the newest duplicate so counters and routing stay stable',()=>{const older={...record,updatedAt:'2025-12-31T00:00:00Z'};const newer={...record,updatedAt:'2026-01-02T00:00:00Z',subject:{...record.subject,title:'Newest'}};expect(normalizeNotifications([older,newer,older])).toEqual([newer])})});
