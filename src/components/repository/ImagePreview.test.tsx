import { render,screen } from '@testing-library/react';import { beforeEach,describe,expect,it,vi } from 'vitest';import { ImagePreview } from './ImagePreview';
describe('image preview',()=>{beforeEach(()=>{vi.stubGlobal('URL',{createObjectURL:vi.fn(()=> 'blob:test'),revokeObjectURL:vi.fn()});});it('routes PNG bytes into the safe image viewer and cleans resources',()=>{const view=render(<ImagePreview file={{path:'a.png',text:null,byteSize:10,mimeType:'image/png',contentBase64:'AA=='}} mode="fit" zoom={1} onMode={()=>{}} onZoom={()=>{}}/>);expect(screen.getByRole('img',{name:'a.png'})).toHaveAttribute('src','blob:test');view.unmount();expect(URL.revokeObjectURL).toHaveBeenCalledWith('blob:test');});it('does not decode oversized images',()=>{render(<ImagePreview file={{path:'huge.jpg',text:null,byteSize:6_000_000,mimeType:'image/jpeg'}} mode="fit" zoom={1} onMode={()=>{}} onZoom={()=>{}}/>);expect(screen.getByText('Oversized image preview limited')).toBeInTheDocument();expect(URL.createObjectURL).not.toHaveBeenCalled();});});
